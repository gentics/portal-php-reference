{
  rootNode {
    children(lang: ["de", "en"], filter: {schema: {or: [{is: %folderSchema%}, {is: %contentSchema%}]}}) {
      elements {
        ...navigation
        children(lang: ["de", "en"], filter: {schema: {or: [{is: %folderSchema%}, {is: %contentSchema%}]}}) {
          elements {
            ...navigation
            children(lang: ["de", "en"], filter: {schema: {or: [{is: %folderSchema%}, {is: %contentSchema%}]}}) {
              elements {
                ...navigation
                children(lang: ["de", "en"], filter: {schema: {or: [{is: %folderSchema%}, {is: %contentSchema%}]}}) {
                  elements {
                    ...navigation
                    children(lang: ["de", "en"], filter: {schema: {or: [{is: %folderSchema%}, {is: %contentSchema%}]}}) {
                      elements {
                        ...navigation
                        children(lang: ["de", "en"], filter: {schema: {or: [{is: %folderSchema%}, {is: %contentSchema%}]}}) {
                          elements {
                            ...navigation
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
